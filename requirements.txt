anthropic>=0.40.0
pypdf>=4.0.0
python-docx>=1.0.0
python-pptx>=0.6.23
rich>=13.0.0
pydantic>=2.0.0
pyyaml>=6.0
markdown>=3.5
pymdown-extensions>=10.7
beautifulsoup4>=4.12
genanki>=0.13
# Optional: PDF OCR stack — auto-triggers when extraction returns empty
# / mostly-image text. System deps: `brew install tesseract poppler`
# (or apt-get install tesseract-ocr poppler-utils).
pytesseract>=0.3.10
pdf2image>=1.17
# Optional secondary text extractor — handles broken font maps better
# than pypdf. We try it before falling all the way down to OCR.
pdfplumber>=0.11
# Local-mode runtime: huggingface_hub fetches open-weight models on demand.
# The actual inference engine (mlx-lm on Apple Silicon, llama-cpp-python
# elsewhere) is installed at first run by bart/local_runtime/installer.py
# so users on API/subscription mode don't pay for unused dependencies.
huggingface_hub>=0.24
psutil>=5.9
